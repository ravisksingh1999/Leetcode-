class Solution {
public:
    int pivotInteger(int n) {
        int sum=0; 
        sum=n*(n+1)/2;
        for(int i=1; i*i<=sum; i++)
        {
            if(i*i==sum) return i;
        }
        return -1;

    }
};
